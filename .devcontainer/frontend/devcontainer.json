{
	"name": "Dev Container",
	"dockerComposeFile": "../docker-compose.yml",
	"service": "frontend",
	"workspaceFolder": "/workspaces/payment_system",
	
	"features": {},
	
	"customizations": {
		"vscode": {
			"extensions": []
		}
	},

	// TODO: migerateもやっちゃいたいけどdbサービスの立ち上がりが間に合っていない
	// "postCreateCommand": "cd /workspaces/payment-system && pip install -r requirements.txt && cd payment_system && python manage.py migrate",
	"forwardPorts": [],
	
	"remoteUser": "root"
}
